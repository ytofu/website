# Appstream User

Manage Appstream User resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_appstream_user:
    example:
      authentication_type: USERPOOL
      user_name: EMAIL
      first_name: FIRST NAME
      last_name: LAST NAME
```
