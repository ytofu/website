# Lightsail Bucket

Manage Lightsail Bucket resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_lightsail_bucket:
    example:
      name: example-bucket
      bundle_id: small_1_0
```
