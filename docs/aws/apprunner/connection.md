# App Runner Connection

Create source repository connections using ytofu YAML.

## GitHub Connection

```yaml
resource:
  aws_apprunner_connection:
    example:
      connection_name: example
      provider_type: GITHUB
```
