# Codeconnections Connection

Manage Codeconnections Connection resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_codeconnections_connection:
    example:
      name: example-connection
      provider_type: Bitbucket
```
