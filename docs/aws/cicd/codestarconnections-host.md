# Codestarconnections Host

Manage Codestarconnections Host resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_codestarconnections_host:
    example:
      name: example-host
      provider_endpoint: "https://example.com"
      provider_type: GitHubEnterpriseServer
```
