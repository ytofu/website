# Scheduler Schedule Group

Manage Scheduler Schedule Group resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_scheduler_schedule_group:
    example:
      name: my-schedule-group
```
