# Schemas Registry

Manage Schemas Registry resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_schemas_registry:
    test:
      name: my_own_registry
      description: A custom schema registry
```
