# Wafregional Regex Pattern Set

Manage Wafregional Regex Pattern Set resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_wafregional_regex_pattern_set:
    example:
      name: example
      regex_pattern_strings: 
        - one
        - two
```
