# Lakeformation Identity Center Configuration

Manage Lakeformation Identity Center Configuration resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_lakeformation_identity_center_configuration:
    example:
      instance_arn: example-identity_center_instance_arn

data:
  aws_ssoadmin_instances:
    example:
```
