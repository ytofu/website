# Quicksight Role Membership

Manage Quicksight Role Membership resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_quicksight_role_membership:
    example:
      member_name: example-group
      role: READER
```
