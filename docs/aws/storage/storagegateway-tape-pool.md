# Storagegateway Tape Pool

Manage Storagegateway Tape Pool resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_storagegateway_tape_pool:
    example:
      pool_name: example
      storage_class: GLACIER
```
