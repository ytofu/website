# SES Active Receipt Rule Set

Manage SES Active Receipt Rule Set resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_ses_active_receipt_rule_set:
    main:
      rule_set_name: primary-rules
```
