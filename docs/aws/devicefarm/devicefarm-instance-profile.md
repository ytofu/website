# Devicefarm Instance Profile

Manage Devicefarm Instance Profile resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_devicefarm_instance_profile:
    example:
      name: example
```
