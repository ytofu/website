# Cloudfront Key Value Store

Manage Cloudfront Key Value Store resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_cloudfront_key_value_store:
    example:
      name: ExampleKeyValueStore
      comment: This is an example key value store
```
