# Cloudfront Anycast IP List

Manage Cloudfront Anycast IP List resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_cloudfront_anycast_ip_list:
    example:
      name: example-list
      ip_count: 21
```
