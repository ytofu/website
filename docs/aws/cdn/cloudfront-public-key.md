# Cloudfront Public Key

Manage Cloudfront Public Key resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_cloudfront_public_key:
    example:
      comment: test public key
      encoded_key: file-content
      name: test_key
```
