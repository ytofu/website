# Cloudfront Origin Access Identity

Manage Cloudfront Origin Access Identity resources using ytofu YAML.

## Basic Example

```yaml
resource:
  aws_cloudfront_origin_access_identity:
    example:
      comment: Some comment
```
